"""Example pipeline stub — replace with real steps."""

from data_builder.agent.runner import run_agent_stub


def run_example_pipeline() -> dict:
    return run_agent_stub(payload={"pipeline": "example"})
